spring:
  application:
    name: product-service
  h2:
    console:
      enabled: true
  datasource:
    url: jdbc:h2:mem:test
    username: sa
    password: ''
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: create

server:
  port: 8081
